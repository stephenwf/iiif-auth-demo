export default function Kiosk() {
  // For the Kiosk interaction pattern, the value of the @id property is the URI of a service that must set an access
  // cookie and then immediately close its window or tab without user interaction.
  return <div>Kiosk</div>
}
